"""Test package.

Adding this file ensures pytest imports test modules with fully-qualified names,
avoiding name collisions between modules with the same basename (e.g. multiple
`test_client.py` files in different folders).
"""

