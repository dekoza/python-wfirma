"""Async OAuth1 header tests.

This module aggregates tests to avoid pytest import-mismatch issues caused by
multiple test modules with the same basename across different folders.
"""

from __future__ import annotations

# (intentionally left empty; file deprecated)
