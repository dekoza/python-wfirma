API Reference
=============

This library includes a scraped API specification in ``docs/api_spec.json`` and a human-readable
reference in ``docs/api_reference.md``.

The Sphinx site documents the Python client API and core concepts (configuration, auth, errors).
For the full endpoint list, refer to the generated Markdown file.
